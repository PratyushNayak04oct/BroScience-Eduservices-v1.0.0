import LibraryListing from "@/components/library/LibraryListing";

export const metadata = {
  title: "Digital Library — BroScience Eduservices",
  description:
    "Access free and premium study materials — notes, formula sheets, practice papers, and study tools for every subject.",
};

export default function LibraryPage() {
  return <LibraryListing />;
}
